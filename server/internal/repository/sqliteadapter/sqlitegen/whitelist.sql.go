// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: whitelist.sql

package sqlitegen

import (
	"context"
)

const addToWhitelist = `-- name: AddToWhitelist :exec
INSERT OR IGNORE INTO whitelist (twitch_user_id) VALUES (?)
`

func (q *Queries) AddToWhitelist(ctx context.Context, twitchUserID string) error {
	_, err := q.db.ExecContext(ctx, addToWhitelist, twitchUserID)
	return err
}

const isWhitelisted = `-- name: IsWhitelisted :one
SELECT COUNT(*) > 0 FROM whitelist WHERE twitch_user_id = ?
`

func (q *Queries) IsWhitelisted(ctx context.Context, twitchUserID string) (bool, error) {
	row := q.db.QueryRowContext(ctx, isWhitelisted, twitchUserID)
	var column_1 bool
	err := row.Scan(&column_1)
	return column_1, err
}

const listWhitelist = `-- name: ListWhitelist :many
SELECT twitch_user_id, added_at FROM whitelist ORDER BY added_at DESC
`

func (q *Queries) ListWhitelist(ctx context.Context) ([]Whitelist, error) {
	rows, err := q.db.QueryContext(ctx, listWhitelist)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Whitelist{}
	for rows.Next() {
		var i Whitelist
		if err := rows.Scan(&i.TwitchUserID, &i.AddedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const removeFromWhitelist = `-- name: RemoveFromWhitelist :exec
DELETE FROM whitelist WHERE twitch_user_id = ?
`

func (q *Queries) RemoveFromWhitelist(ctx context.Context, twitchUserID string) error {
	_, err := q.db.ExecContext(ctx, removeFromWhitelist, twitchUserID)
	return err
}
