// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: titles.sql

package pggen

import (
	"context"
)

const linkStreamTitle = `-- name: LinkStreamTitle :exec
INSERT INTO stream_titles (stream_id, title_id) VALUES ($1, $2) ON CONFLICT DO NOTHING
`

type LinkStreamTitleParams struct {
	StreamID string `json:"stream_id"`
	TitleID  int64  `json:"title_id"`
}

func (q *Queries) LinkStreamTitle(ctx context.Context, arg LinkStreamTitleParams) error {
	_, err := q.db.Exec(ctx, linkStreamTitle, arg.StreamID, arg.TitleID)
	return err
}

const linkVideoTitle = `-- name: LinkVideoTitle :exec
INSERT INTO video_titles (video_id, title_id) VALUES ($1, $2) ON CONFLICT DO NOTHING
`

type LinkVideoTitleParams struct {
	VideoID int64 `json:"video_id"`
	TitleID int64 `json:"title_id"`
}

func (q *Queries) LinkVideoTitle(ctx context.Context, arg LinkVideoTitleParams) error {
	_, err := q.db.Exec(ctx, linkVideoTitle, arg.VideoID, arg.TitleID)
	return err
}

const listTitlesForStream = `-- name: ListTitlesForStream :many
SELECT t.id, t.name, t.created_at FROM titles t
INNER JOIN stream_titles st ON st.title_id = t.id
WHERE st.stream_id = $1
ORDER BY t.id
`

func (q *Queries) ListTitlesForStream(ctx context.Context, streamID string) ([]Title, error) {
	rows, err := q.db.Query(ctx, listTitlesForStream, streamID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Title{}
	for rows.Next() {
		var i Title
		if err := rows.Scan(&i.ID, &i.Name, &i.CreatedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTitlesForVideo = `-- name: ListTitlesForVideo :many
SELECT t.id, t.name, t.created_at FROM titles t
INNER JOIN video_titles vt ON vt.title_id = t.id
WHERE vt.video_id = $1
ORDER BY vt.linked_at
`

// Ordered by linked_at, not titles.id — the dedup-row id reflects
// creation order across the whole titles table, not the order a
// specific video saw each title. Without this the history UI
// misorders titles whenever a stream reuses a name from a prior
// broadcast.
func (q *Queries) ListTitlesForVideo(ctx context.Context, videoID int64) ([]Title, error) {
	rows, err := q.db.Query(ctx, listTitlesForVideo, videoID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Title{}
	for rows.Next() {
		var i Title
		if err := rows.Scan(&i.ID, &i.Name, &i.CreatedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertTitle = `-- name: UpsertTitle :one
INSERT INTO titles (name) VALUES ($1)
ON CONFLICT (name) DO UPDATE SET name = EXCLUDED.name
RETURNING id, name, created_at
`

func (q *Queries) UpsertTitle(ctx context.Context, name string) (Title, error) {
	row := q.db.QueryRow(ctx, upsertTitle, name)
	var i Title
	err := row.Scan(&i.ID, &i.Name, &i.CreatedAt)
	return i, err
}
