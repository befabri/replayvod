# Backend origin for local dashboard development only. Leave empty for normal
# builds: Docker and production serve the dashboard from the Go server on the
# same origin. The Vite dev proxy forwards /api/* and /trpc/* to the Go server.
VITE_API_URL=
# Optional local override if you intentionally bypass the dev proxy:
# VITE_API_URL=http://localhost:8080
